{
  "name": "raid-manager-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute raid-manager --file=./src/schema.sql",
    "db:init:remote": "wrangler d1 execute raid-manager --remote --file=./src/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
